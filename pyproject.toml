[project]
name = "vine-social-tokyo"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.110,<0.116",
  "pydantic>=2,<3",
  "python-dotenv>=1.0,<2",
]

[tool.vercel]
# Local hint; production uses api/index.py → backend.server:app
entrypoint = "backend.server:app"
